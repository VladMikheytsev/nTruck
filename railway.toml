[build]
builder = "nixpacks"
buildCommand = "cd railway-backend && npm ci"

[deploy]
startCommand = "cd railway-backend && node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
PORT = "3001"
