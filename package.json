{
  "name": "ntruck-railway",
  "version": "1.0.0",
  "description": "nTruck GPS Backend for Railway",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "echo 'No build step required'",
    "dev": "cd backend && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
